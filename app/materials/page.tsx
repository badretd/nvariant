import type { Metadata } from "next"; import Link from "next/link"; import { materials } from "@/content/registry"; import { formatDate } from "@/components/material";
export const metadata: Metadata = { title: "Материалы", description: "Все самостоятельные материалы N-вариант.", alternates: { canonical: "/materials" } };
export default function MaterialsPage() { return <main id="main" className="page-shell archive"><header className="page-title"><span className="eyebrow">Полный индекс</span><h1>Материалы</h1></header><div className="material-list">{materials.filter((m) => m.showInMaterialsArchive).map((m) => <article key={m.slug}><div><time>{formatDate(m.publishedAt)}</time><span>{m.tags.join(" · ")}</span></div><h2><Link href={`/materials/${m.slug}`}>{m.title}</Link></h2><p>{m.description}</p><small>{m.readingTime} мин чтения</small></article>)}</div></main>; }
