import type { Metadata } from "next"; import Image from "next/image"; import Link from "next/link";
import { issues, issueMaterials } from "@/content/registry"; import { formatDate } from "@/components/material";
export const metadata: Metadata = { title: "Выпуски", description: "Архив выпусков журнала N-вариант.", alternates: { canonical: "/issues" } };
export default function IssuesPage() { return <main id="main" className="archive page-shell"><header className="page-title"><span className="eyebrow">Архив</span><h1>Выпуски</h1><p>Редакционные композиции для последовательного чтения.</p></header>{issues.map((i) => <article className="issue-row" key={i.slug}><Image src="/media/issue-1-cover.svg" alt="" width={300} height={400} /><div><span>№{i.number} · {formatDate(i.publishedAt)}</span><h2><Link href={`/issues/${i.slug}`}>{i.title}</Link></h2><p>{i.description}</p><small>{issueMaterials(i).length} материала · {issueMaterials(i).reduce((n, m) => n + m.readingTime, 0)} мин</small></div></article>)}</main>; }
